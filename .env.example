# Supabase Configuration
# Get these values from your Supabase project settings:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api
#
# API Keys Documentation: https://supabase.com/docs/guides/api/api-keys

# Your Supabase project URL
PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Publishable Key (sb_publishable_...)
# Safe to expose in client-side code
# Get this from: Supabase Dashboard → Settings → API → Project API keys → publishable key
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Secret Key (sb_secret_...)
# NEVER expose in client-side code! Only use in secure backend environments.
# This is NOT used by the client - only for server-side operations.
# SUPABASE_SECRET_KEY=

# ============================================================================
# Firebase Cloud Messaging (Push Notifications)
# ============================================================================
# Get these values from Firebase Console:
# https://console.firebase.google.com/project/YOUR_PROJECT/settings/general
#
# 1. Create a Firebase project
# 2. Add a web app
# 3. Copy the config values below
#
# For VAPID key:
# Firebase Console → Project Settings → Cloud Messaging → Web Push certificates

PUBLIC_FIREBASE_API_KEY=
PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
PUBLIC_FIREBASE_PROJECT_ID=your-project-id
PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
PUBLIC_FIREBASE_APP_ID=

# VAPID key for web push (required for push notifications)
# Generate in Firebase Console → Cloud Messaging → Web Push certificates
PUBLIC_FIREBASE_VAPID_KEY=

