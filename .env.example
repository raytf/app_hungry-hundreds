# Supabase Configuration
# Get these values from your Supabase project settings:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api
#
# API Keys Documentation: https://supabase.com/docs/guides/api/api-keys

# Your Supabase project URL
PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# =============================================================================
# NEW API KEYS (Recommended - June 2025+)
# =============================================================================
# These are the new API key formats that will replace the legacy JWT-based keys.
# Use the Publishable key for client-side code (web, mobile, CLI).
# Use the Secret key for server-side code only (Edge Functions, servers).

# Publishable Key (sb_publishable_...)
# Safe to expose in client-side code, replaces 'anon' key
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Secret Key (sb_secret_...)
# NEVER expose in client-side code! Only use in secure backend environments.
# This is NOT used by the client - only for server-side operations.
# SUPABASE_SECRET_KEY=

# =============================================================================
# LEGACY API KEYS (Still Supported)
# =============================================================================
# These JWT-based keys are being deprecated. Migrate to new keys above.
# Can still be used during the transition period (until late 2026).

# Legacy anon key (JWT format)
# Falls back to this if PUBLIC_SUPABASE_PUBLISHABLE_KEY is not set
PUBLIC_SUPABASE_ANON_KEY=

